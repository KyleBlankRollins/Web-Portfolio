# Test Hook

This is a test file to verify our git pre-commit hook works properly with the new TypeScript linting script.

The file contains some intentionally mispelled words like "recieve" and "occurance" to trigger Vale errors.